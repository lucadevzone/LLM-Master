FATTO - Quando mi collego su http://localhost:3000 mi appare una pagina "vecchia" senza alcun senso. Dovrebbe indirizzarmi a http://localhost:3000/login se non sono loggato, oppure a http://localhost:3000/lobby se sono loggato come giocatore e infine a http://localhost:3000/admin se sono loggato come admin

FATTO - Servirebbe una procedura interna per cambiare la password (aggiunto bottone "Password" + modal in lobby.html e admin.html; endpoint POST /api/auth/change-password)

Quando si crea una nuova stanza appare una finestra con alcuni parametri da selezionare. Il campo seleziona giocatori è esteticamente errato in quanto le combo box sono troppo a destra e non sono allineate tra di loro. Stessa cosa per il nome dei giocatori

NON-ISSUE - La cartella players (data/users/players/) non è una duplicazione di data/rooms/{id}/characters/: servono a cose diverse. players contiene gli account dei giocatori (email, passwordHash, salt, inviteCode) ed è il registro globale usato dall'autenticazione, indipendente dalle stanze. characters contiene le schede personaggio RPG (caratteristiche, abilità, backstory) ed è specifica per ogni stanza/avventura. Un giocatore può avere un personaggio diverso in stanze diverse. La nota nasceva da una confusione durante la migrazione dei file, non da un problema architetturale reale.

FATTO - all'avvio della campagna il custode ha narrato un'introduzione non adatta (dovremmo essere nella fase "Presentare ambientazione, PG e eventuali Antefatti"). Aggiunti file custode/avvio.md e custode/inizio_sessione.md; triggerSessionStart imposta correttamente il cycle_phase.

FATTO - Il custode non stava rispondendo quando il floor tornava libero (sistema bloccato). Aggiunta direttiva PASS: il custode può decidere di non intervenire quando i giocatori conversano tra loro, lasciando la parola libera.

FATTO - Le nuvolette di testo dei giocatori dovrebbero avere un colore diverso. Color picker nel tab Sessione; colore salvato in localStorage (chiave llm-player-color); aggiorna la CSS var --player-color che governa border e label. Il background della bubble si adatta automaticamente via color-mix(). Persiste tra sessioni diverse.

FATTO - Comando rapido: il testo grezzo non appare più in chat (né per il giocatore attivo né per gli altri). Lato server: il broadcast player_action viene saltato se quick_action=true. Lato prompt: aggiunta regola in reagire_dichiarazioni.md che vieta al GM di scrivere il dialogo del PG — deve narrare solo la reazione dell'ambiente/PNG.

FATTO - Il pg ha scritto (bene salutiamo il sign Dumont e ci congediamo) questo dovrebbe portare ad una nuova scena. Creazione di scene/002.json e aggiornamento diary.txt ora gestiti dal server tramite direttiva NEW_SCENE.

FATTO - Siamo passati al turno fisso: le scelte rapide spuntavano a tutti i giocatori (dovrebbero solo al giocatore di turno). ui_hints ora inviati solo al giocatore attivo via sendTo. Bug "Il pavimento non è libero" risolto con controllo alreadyAssigned in player-turn.


FATTO - gestione della connessione nella room e sincronizzazione con i client dei giocatori. Centralizzato nel server: endpoint GET /api/sessions/:id/connected-players; il client fa query al server ogni volta che si apre il tab "Sessione". Gli eventi SSE player_connected/player_disconnected aggiornano la lista in background. Il nome viene mostrato come <nome_pg> (Tu) per se stessi; per gli altri manca ancora il nome_giocatore tra parentesi (da aggiungere quando il sistema di autenticazione espone il nome del giocatore).

FATTO - small changes in the UI
FATTO - Cambia il bottone "Interrompi" in "Fuori Turno"
FATTO - Nella TextArea metterei solo il suggerimento "Cosa fai?"
FATTO - Accanto a raise_hand metterei un altro button che si attiva quando il PG deve fare una prova: label "⬡ NomeSkill (d100)", appare solo quando il GM richiede un tiro, scompare dopo il click
FATTO - Nella scheda Diario metterei due text area: "Diario di sessione" (non editabile, popolato da eventi) e "Note personali" (editabile, salvato in localStorage per sessione)
FATTO - In Sessione rinominiamo "chiudi sessione" in "proponi di chiudere la sessione"

FATTO - riorganizzazione dei file e delle informazioni
FATTO - Quando l'admin crea una nuova stanza, il sistema crea una cartella data/rooms/{id}/ con room.json, diary.txt vuoto e sottocartella npcs/. Migrazione automatica dei room file in formato vecchio (flat .json) alla prima lettura.
FATTO - Il primo file generato è diary.txt (vuoto). API: GET/POST /api/rooms/:id/diary per leggere e appendere voci.
FATTO - Le schede PG ora includono i campi personal_diary (array, auto-popolato) e player_notes (stringa, editabile). Il campo player_notes è salvato in localStorage lato client e sarà sincronizzato con la scheda nella sessione successiva.
FATTO - Le schede PNG sono strutturate con name, role, description, stats, events[] e knowledge[]. API completa: GET/POST /api/rooms/:id/npcs, PATCH /:npcId, POST /:npcId/event, POST /:npcId/knowledge. La creazione automatica durante la narrazione (quando il GM introduce un PNG) è da implementare nel responseParser.


FATTO - durante la sessione il custode ha un "ciclo".
Quando tutti i giocatori sono collegati
FATTO - se è la prima sessione in assoluto (avvio dell'avventura) si parte con "Presentare ambientazione, PG e eventuali Antefatti" per poi entrare nel ciclo del custode.
FATTO - se invece non è la prima sessione, il custode "Fa una breve sintesi degli eventi principali e delle ultime giocate" per poi entrare nel ciclo del custode
FATTO - Ciclo del custode: impostare la scena --> coinvolgere i PG --> reagire alle dichiarazioni.

FATTO - In "impostare la scena": regole in custode/impostare_scena.md, direttive NEW_SCENE e UPDATE_SCENE per gestire le scene (salvate in sessions/{id}/scenes/NNN.json). La scena corrente viene iniettata nel system prompt.
FATTO - Sussurro privato: direttiva WHISPER { character_name, message } — il messaggio arriva solo al giocatore destinatario via SSE sendTo, con stile grafico distinto (sfondo scuro, bordo viola, corsivo).

FATTO - In "coinvolgere i PG": regole in custode/coinvolgere_pg.md. Direttive ASSIGN_TURN (assegna floor a un PG per nome) e OPEN_FLOOR (parola libera). Il GM decide quale dei due usare.

FATTO - In "reagire alle dichiarazioni": regole in custode/reagire_dichiarazioni.md. Fase attivata automaticamente dal server quando un giocatore prende il floor. Direttive UPDATE_SCENE, SET_CYCLE_PHASE per tornare al ciclo.
FATTO - Timeout proattivo: se i giocatori non agiscono per GM_PROACTIVE_TIMEOUT_MS (default 5 min), il GM interviene autonomamente con un messaggio di stimolo narrativo.
